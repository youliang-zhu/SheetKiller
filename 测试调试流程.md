# SheetKiller 测试调试流程

本文档用于产品测试阶段。目标是减少“截图 + 口头描述”的猜测成本，让每次问题反馈都带上可复现的调试证据。

## 推荐测试流程

1. 在 `dynamic-fill-solution` 目录执行：

   ```powershell
   npm run build
   ```

2. 打开 `chrome://extensions`，对 SheetKiller 点击“重新加载”。

   不要移除扩展。移除扩展会清掉本地资料、设置和调试记录，后续还要重新输入 API Key 和资料。

3. 刷新目标网申页面。

4. 打开 SheetKiller 组件，点击“扫描并生成计划”，再点击“开始填写”。

5. 如果结果不符合预期，打开 SheetKiller 的“设置”，在“测试调试包”区域点击“导出最近调试包”。

6. 反馈问题时提供：

   - 页面截图
   - 导出的 `sheetkiller-debug-*.json`
   - 一句话描述你认为哪里不对，例如“学校名称没有填写”“所有地区下拉失败”“专业类别看起来填了但被判红”

## 调试包包含什么

调试包会自动记录最近 20 次测试，包括：

- 页面 URL、domain、title
- 扩展版本和浏览器 userAgent
- 扫描到的字段：label、placeholder、name、id、inputType、options、DOM 摘要
- 规划结果：sourcePath、strategy、confidence、safety、reason、脱敏后的 expected value
- 执行结果：每个字段的 status、reason、expectedMasked、actualMasked
- 执行细节：填之前/填之后的值、元素 tag/type/class、readonly/disabled、optionsCount、异常信息

## 隐私说明

调试包不会包含 API Key。

以下值会脱敏：

- 手机号
- 邮箱
- 证件号
- 银行卡号

姓名、学校、公司、项目描述等普通资料当前会保留，用于判断字段匹配和填写逻辑。如果需要发给外部人员，请先手动检查 JSON 内容。

## 当前已知重点排查方向

后续调试会重点看以下字段：

- `scan.fields`：字段有没有被正确扫描，inputType 是否准确。
- `plan.items`：LLM/启发式规划是否给了正确 sourcePath 和 strategy。
- `execution.debugItems`：填之前、填之后和校验实际值是否一致。
- `execution.reports`：失败是填充失败，还是填了但 verifier 判定不匹配。

如果某个下拉失败，优先看对应 debug item 的：

- `element.inputType`
- `element.tagName`
- `element.optionsCount`
- `beforeMasked`
- `afterMasked`
- `reason`
- `exception`
