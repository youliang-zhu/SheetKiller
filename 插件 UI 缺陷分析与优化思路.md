# SheetKiller 插件 UI 缺陷分析与优化思路

本文档聚焦浏览器扩展弹窗 UI，不覆盖资料填写页的字段规划与表单细节。资料填写页是“资料管理工作台”，插件弹窗应该是“当前网页操作台”：帮助用户判断当前页面是否可扫描、资料和 AI 设置是否就绪，以及下一步该执行什么。

## 设计目标

插件弹窗需要在很小的空间里回答三个问题：

1. 当前页面能不能连接和扫描。
2. 当前资料、AI 设置、填写计划是否就绪。
3. 用户现在最应该点击哪一个操作。

所以插件 UI 不应照搬资料填写页的复杂表单结构，而应复用同一套品牌色、圆角、阴影、按钮、状态和文案规则，形成同源但不同职责的体验。

## 当前主要问题

### 1. 插件像缩小版页面，而不是操作台

当前 popup 使用了较多大圆角卡片：顶部资料进度卡、流程卡、按钮区、规则兜底区。视觉上已经接近资料页的卡片语言，但在 420px 宽的弹窗里显得层级偏重、纵向空间消耗过快。

问题不是卡片本身，而是弹窗里每个模块都被做成独立大卡片，导致用户打开插件后需要先理解布局，而不是立刻知道下一步。

建议将 popup 改成更紧凑的“状态头 + 主操作区 + 结果区 + 底部工具栏”结构，减少大卡片数量。

### 2. 与资料填写页统一了颜色，但没有统一组件语法

popup 和资料页都使用了 `#F4F7FC`、`#FFFFFF`、`#1D4ED8`、`#64748B` 等色板，这是对的。但两边的组件语法仍不完全一致：

- popup 的流程步骤是胶囊 pill。
- 资料页是左侧导航 + 右侧表单区。
- popup 的按钮、提示、错误、预览项没有抽象成与资料页共享的组件规则。

这会造成“看起来像同一个品牌，但不像同一个产品系统”的感觉。

建议建立最小设计系统：`Surface`、`PrimaryButton`、`SecondaryButton`、`StatusPill`、`ProgressBar`、`InlineAlert`、`ActionRow`。资料页和插件页都使用这些基础组件，只在密度和布局上区分。

### 3. 工作流步骤目前更像装饰，不像状态

`扫描 / 规划 / 填写 / 复核` 四个步骤目前以 pill 形式展示，但没有充分承担状态解释：

- idle 状态下四个步骤看起来像可点击 tab，但实际上不是。
- error 状态下步骤无法解释失败发生在哪一步。
- planned 状态下用户不能快速看出“下一步是开始填写还是预览计划”。

建议把它改成轻量进度线或状态清单，并明确当前节点：

- 未开始：只显示“准备扫描当前页面”。
- 扫描中：高亮“扫描页面”。
- 已生成计划：显示“已找到 X 个字段，可填写 Y 个，需复核 Z 个”。
- 填写中：显示“正在填写，最终提交已拦截”。
- 异常：显示失败节点和恢复操作。

### 4. 错误提示缺少恢复路径

当前错误提示能告诉用户无法连接页面，但对普通用户而言，“刷新页面后重试”仍然不够具体。尤其插件场景里常见失败原因不同：

- 当前页面是 `chrome://`、扩展页、PDF 预览等不可注入页面。
- 内容脚本还没连接。
- 当前页面刚刷新，扩展上下文失效。
- 资料缺失。
- AI 设置缺失。
- 页面字段太少或没有表单。

建议错误区不只显示红色信息，还要显示 1-2 个恢复按钮，例如：

- `重新连接并扫描`
- `刷新页面后重试`
- `打开设置`
- `编辑资料`

错误文案保持中文、短句、行动导向。

### 5. 主操作层级还可以更清晰

当前弹窗同时出现：

- `扫描并生成计划`
- `开始填写`
- `预览计划`
- `编辑资料`
- `设置`
- `规则填写兜底`

这些操作都合理，但层级应更明确：

- 主按钮：当前状态下唯一最推荐动作。
- 次按钮：资料、设置、预览。
- 高级/兜底操作：收进“更多”或放在底部弱化。

`规则填写兜底` 对普通用户含义不直观，建议改为 `仅按规则填写`，并作为高级备用能力，不与 AI 主流程平级。

### 6. 缺少当前网页上下文

插件弹窗的核心差异是它依附于当前页面，但当前 UI 没有明确展示当前网页状态。用户失败时不知道是资料问题、插件问题，还是网页问题。

建议顶部或主操作区显示当前页面上下文：

- 当前域名，例如 `campus.example.com`
- 页面连接状态：`已连接` / `未连接`
- 表单检测状态：`发现表单` / `未发现可填写字段`

这能把插件从“资料面板”转成“网页操作台”。

### 7. 资料进度卡过大，但信息密度不足

资料进度很重要，但在 popup 里不需要占用一个大卡片。它应该是就绪条件之一，而不是页面主角。

建议把资料状态压缩成一行：

`本地资料 13/23 项 · 57% · 继续补充`

点击该行或旁边按钮进入资料页。这样能保留信息，又给扫描/填写流程腾出空间。

### 8. 预览计划需要更像“复核入口”

当前预览计划是列表项，能看字段和 masked value，但还可以更面向复核：

- 先显示摘要：`可填写 / 需复核 / 跳过`。
- 只默认展示最需要注意的 3 项。
- 提供 `查看全部计划`。
- 对需要复核的字段使用橙色状态，而不是只靠文字。

插件空间有限，预览不应变成完整计划编辑器。完整编辑可以后续放到 dashboard。

### 9. 字体使用应更克制

品牌名可以继续保留轻微漫画感字体，形成识别度。但根据产品 UI 准则，按钮、标签、字段值、状态和错误文案不应使用 display/comic 字体。任务型工具里，字体的职责是降低阅读成本。

建议：

- `SheetKiller` logo 使用 `Comic Neue` 或现有 display 字体。
- 其余 UI 使用 `Segoe UI Variable / system-ui`。
- 保持中文界面，不混用英文流程文案。技术名词如 `API Key`、`Base URL` 可以保留。

## 推荐的信息架构

### 顶部：身份和页面状态

内容：

- `SheetKiller`
- `AI 表单助手`
- 当前域名或 `当前页面`
- 连接状态 pill：`已连接` / `未连接`

目的：让用户一眼知道插件正在服务哪个页面。

### 就绪清单：三项即可

内容：

- 当前页面：`可扫描` / `不可连接`
- 本地资料：`13/23 项`
- AI 设置：`已配置` / `未配置`

目的：提前解释主按钮为什么可用或不可用。

### 主操作区：一个主按钮

按钮随状态变化：

- idle：`扫描当前页面`
- scanning：`正在扫描`
- planned：`开始填写`
- filling：`正在填写`
- done：`查看填写结果`
- error：`重新扫描`

主按钮下面可以放一行小号说明，解释不会最终提交。

### 计划摘要区：只在有计划后出现

内容：

- `发现 X 个字段`
- `可填写 Y 个`
- `需复核 Z 个`
- `跳过 N 个`

下面展示 3 个重点复核项，再提供 `查看全部计划`。

### 底部工具栏

建议底部固定为三类入口：

- `编辑资料`
- `设置`
- `更多`

`更多` 中放 `仅按规则填写`、调试信息、复制错误详情等高级操作。

## 建议视觉方向

继续采用当前已经确认的 Slate Blue AI 风格：

- 背景：`#F4F7FC`
- 卡片：`#FFFFFF`
- 主按钮：`#1D4ED8`
- Hover：`#2563EB`
- Secondary Text：`#64748B`
- Primary Text：`#0F172A`
- Border：`#E6ECF5`
- Success：`#16A34A`
- Warning：`#F59E0B`
- Error：`#DC2626`

但 popup 里需要降低“卡片堆叠感”：

- 外层背景保留浅蓝灰。
- 主内容只保留一个主要白色面板。
- 状态清单用行项目，不再每块都做大卡片。
- 阴影更轻，避免弹窗像浮在另一个浮层上。
- 圆角保持圆润，但控制在 16-24px，避免所有模块都过度圆成胶囊。

## 状态设计规划

建议把 popup 明确拆成以下状态，并为每个状态设计对应文案和按钮：

| 状态 | 用户看到的重点 | 主操作 |
|---|---|---|
| `idle` | 当前页面可扫描，资料/AI 是否就绪 | 扫描当前页面 |
| `connecting` | 正在连接当前页面 | 等待或重试 |
| `scanning` | 正在读取表单字段 | 正在扫描 |
| `planned` | 已生成计划摘要 | 开始填写 |
| `preview` | 重点复核字段 | 开始填写 / 收起 |
| `filling` | 正在填写，最终提交已拦截 | 等待 |
| `done` | 成功、跳过、失败数量 | 查看结果 |
| `error` | 失败原因和恢复动作 | 重新连接并扫描 |

这套状态比单纯展示错误 toast 更可靠，也方便后续测试。

## 实施优先级

### P0：先修正插件职责

目标：让 popup 成为当前网页操作台。

- 加入当前域名和连接状态。
- 将资料进度压缩为就绪清单。
- 主按钮根据状态变化。
- 错误提示增加恢复按钮。

### P1：统一组件系统

目标：让 popup 和资料页像同一套产品。

- 抽出共享 token 和基础组件。
- 统一按钮、状态 pill、进度条、alert、surface 的圆角和阴影。
- 统一中文文案和状态词。

### P2：优化计划预览和复核

目标：让用户在填写前有足够信心，但不被信息淹没。

- 默认展示摘要。
- 高亮需复核字段。
- 展示少量关键项。
- 完整计划放到展开区或 dashboard。

### P3：细化高级能力

目标：保留专业能力，不打扰普通流程。

- `仅按规则填写` 放入更多菜单。
- 调试信息仅在高级区域出现。
- 复制错误详情、重新注入脚本等能力放到排障入口。

## 不建议做的事

- 不要把资料填写页的字段搬到 popup。
- 不要把 popup 做成完整计划编辑器。
- 不要用大量英文流程文案。
- 不要让规则兜底和 AI 主流程同等突出。
- 不要把错误只做成红色警告而不给恢复路径。
- 不要在标签、按钮、数据值里使用漫画字体。
- 不要展示敏感资料原文，尤其手机号、邮箱、证件、API Key。

## 下一步落地建议

下一轮 UI 修改可以按这个范围执行：

1. 重构 popup 信息架构：顶部页面状态、就绪清单、主操作区、计划摘要、底部工具栏。
2. 将 popup 的视觉密度降下来，减少大卡片堆叠。
3. 保持资料页现有结构，只提取共享组件和 token。
4. 为 `idle / planned / filling / done / error` 截图验收，确保每个状态都能解释“发生了什么”和“下一步做什么”。

最终目标是：资料页负责“把我的信息整理好”，插件负责“帮我处理当前网页”。两者共享同一套 SheetKiller 视觉系统，但不承担同一种界面职责。
